[workspace]
members = ["apps/desktop/src-tauri", "crates/mono-api"]
resolver = "2"

# 워크스페이스는 루트 매니페스트에서만 profile을 인식한다 — 멤버의 [profile.*]는 무시됨.
[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
